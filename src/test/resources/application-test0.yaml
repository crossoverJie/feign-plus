feign:
  plus:
    connect-timeout: 11000
    max-idle-connections: 520
    read-timeout: 12000
github:
  url: https://api.github.com
  port: 443

logging:
  level:
    top:
      crossoverjie: debug
spring:
  application:
    name: feign-plus-test
